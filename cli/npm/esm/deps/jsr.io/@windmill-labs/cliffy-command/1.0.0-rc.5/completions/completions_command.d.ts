import { Command } from "../command.js";
/** Generates shell completion scripts for various shells. */
export declare class CompletionsCommand extends Command<void, void, void, [], {
    name: string;
}> {
    #private;
    constructor(cmd?: Command);
}
//# sourceMappingURL=completions_command.d.ts.map