import { Command } from "../command.js";
/** Generates bash completions script. */
export declare class BashCompletionsCommand extends Command<void, void, {
    name: string;
}> {
    #private;
    constructor(cmd?: Command);
}
//# sourceMappingURL=bash.d.ts.map