import * as dntShim from "./_dnt.shims.js";
export interface ReadLineResult {
    bytes: Uint8Array;
    eol: boolean;
}
export declare class BufferFullError extends Error {
    partial: Uint8Array;
    name: string;
    constructor(partial: Uint8Array);
}
/** BufReader implements buffering for a Reader object. */
export declare class BufReader {
    #private;
    constructor(rd: dntShim.Deno.Reader, size?: number);
    buffered(): number;
    readLine(strip?: boolean): Promise<{
        bytes: Uint8Array;
        eol: boolean;
    } | null>;
    readSlice(delim: number): Promise<Uint8Array | null>;
}
