"use strict";
// Copyright 2018-2024 the Deno authors. All rights reserved. MIT license.
// This module is browser compatible.
var _StringWriter_chunks, _StringWriter_byteLength, _StringWriter_cache;
Object.defineProperty(exports, "__esModule", { value: true });
exports.StringWriter = void 0;
const tslib_1 = require("tslib");
const decoder = new TextDecoder();
/**
 * Writer utility for buffering string chunks.
 *
 * @example
 * ```ts
 * import {
 *   copyN,
 *   StringReader,
 *   StringWriter,
 * } from "https://deno.land/std@$STD_VERSION/io/mod.ts";
 * import { copy } from "https://deno.land/std@$STD_VERSION/streams/copy.ts";
 *
 * const w = new StringWriter("base");
 * const r = new StringReader("0123456789");
 * await copyN(r, w, 4); // copy 4 bytes
 *
 * // Number of bytes read
 * console.log(w.toString()); //base0123
 *
 * await copy(r, w); // copy all
 * console.log(w.toString()); // base0123456789
 * ```
 *
 * **Output:**
 *
 * ```text
 * base0123
 * base0123456789
 * ```
 *
 * @deprecated (will be removed after 1.0.0) Use the [Web Streams API]{@link https://developer.mozilla.org/en-US/docs/Web/API/Streams_API} instead.
 */
class StringWriter {
    constructor(base = "") {
        Object.defineProperty(this, "base", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: base
        });
        _StringWriter_chunks.set(this, []);
        _StringWriter_byteLength.set(this, 0);
        _StringWriter_cache.set(this, void 0);
        const c = new TextEncoder().encode(base);
        tslib_1.__classPrivateFieldGet(this, _StringWriter_chunks, "f").push(c);
        tslib_1.__classPrivateFieldSet(this, _StringWriter_byteLength, tslib_1.__classPrivateFieldGet(this, _StringWriter_byteLength, "f") + c.byteLength, "f");
    }
    write(p) {
        return Promise.resolve(this.writeSync(p));
    }
    writeSync(p) {
        tslib_1.__classPrivateFieldGet(this, _StringWriter_chunks, "f").push(new Uint8Array(p));
        tslib_1.__classPrivateFieldSet(this, _StringWriter_byteLength, tslib_1.__classPrivateFieldGet(this, _StringWriter_byteLength, "f") + p.byteLength, "f");
        tslib_1.__classPrivateFieldSet(this, _StringWriter_cache, undefined, "f");
        return p.byteLength;
    }
    toString() {
        if (tslib_1.__classPrivateFieldGet(this, _StringWriter_cache, "f")) {
            return tslib_1.__classPrivateFieldGet(this, _StringWriter_cache, "f");
        }
        const buf = new Uint8Array(tslib_1.__classPrivateFieldGet(this, _StringWriter_byteLength, "f"));
        let offs = 0;
        for (const chunk of tslib_1.__classPrivateFieldGet(this, _StringWriter_chunks, "f")) {
            buf.set(chunk, offs);
            offs += chunk.byteLength;
        }
        tslib_1.__classPrivateFieldSet(this, _StringWriter_cache, decoder.decode(buf), "f");
        return tslib_1.__classPrivateFieldGet(this, _StringWriter_cache, "f");
    }
}
exports.StringWriter = StringWriter;
_StringWriter_chunks = new WeakMap(), _StringWriter_byteLength = new WeakMap(), _StringWriter_cache = new WeakMap();
