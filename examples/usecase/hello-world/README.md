# Hello World

The smallest possible Windmill example: a bash script that echoes `hello world`.

## Run it

```sh
sh hello_world.sh
```

Or paste `hello_world.sh` into a new Bash script in Windmill and run it — the
output will be `hello world`.
