export default class default_1 {
    constructor() {
        this.rules = [
            {
                // string
                expression: /".*?[^\\](?:\\\\)*"/,
            },
            {
                // multiline comment
                expression: /#\[((?:.|\s)*?)\]#/,
            },
            {
                // comment
                expression: /#.*/,
            },
        ];
    }
}
default_1.importExpression = /^(?:(?:(\bfrom +(.*?) +)?(\bimport) +(.*?))|(?:(\binclude) +(.*?))) *$/gm;
