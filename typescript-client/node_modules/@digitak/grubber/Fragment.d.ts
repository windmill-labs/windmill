export default class Fragment {
    start: number;
    end: number;
    groups: string[];
    slice: string;
    constructor(source: string, start: number, end: number, groups?: string[]);
    get length(): number;
}
