{
  "db_name": "PostgreSQL",
  "query": "SELECT EXISTS (\n               SELECT 1\n               FROM workspace_settings ws,\n                    jsonb_array_elements(\n                        CASE WHEN jsonb_typeof(ws.git_sync->'repositories') = 'array'\n                             THEN ws.git_sync->'repositories' END\n                    ) r\n               JOIN v2_job_queue q\n                 ON q.id = (r->'auto_pull'->'last_pull_status'->>'job_id')::uuid\n               WHERE ws.workspace_id = $1\n                 AND r->'auto_pull'->'last_pull_status'->>'synced_sha' = $2\n           ) OR EXISTS (\n               SELECT 1\n               FROM v2_job_queue q\n               JOIN v2_job j ON j.id = q.id\n               WHERE q.workspace_id = $1\n                 AND j.kind IN ('dependencies', 'flowdependencies', 'appdependencies')\n           ) as \"in_flight!\"",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "in_flight!",
        "type_info": "Bool"
      }
    ],
    "parameters": {
      "Left": [
        "Text",
        "Text"
      ]
    },
    "nullable": [
      null
    ]
  },
  "hash": "98a7a315fd4ac164da3b2093f1e71bba65e15357f49800916ae60f701ccdee30"
}
