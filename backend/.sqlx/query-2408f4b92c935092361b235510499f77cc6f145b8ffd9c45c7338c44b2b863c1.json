{
  "db_name": "PostgreSQL",
  "query": "UPDATE eval_dataset SET scorers = COALESCE((\n                SELECT jsonb_agg(\n                    CASE WHEN elem->>'path' LIKE ('u/' || $2 || '/%')\n                    THEN jsonb_set(elem, '{path}', to_jsonb(REGEXP_REPLACE(elem->>'path', 'u/' || $2 || '/(.*)', $1 || '/\u0001')))\n                    ELSE elem END)\n                FROM jsonb_array_elements(scorers) elem), '[]'::jsonb)\n           WHERE workspace_id = $3\n             AND EXISTS (SELECT 1 FROM jsonb_array_elements(scorers) e WHERE e->>'path' LIKE ('u/' || $2 || '/%'))",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Text",
        "Text",
        "Text"
      ]
    },
    "nullable": []
  },
  "hash": "2408f4b92c935092361b235510499f77cc6f145b8ffd9c45c7338c44b2b863c1"
}
