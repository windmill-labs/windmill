description: Account info for pushover
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  order:
    - token
    - user
  properties:
    token:
      type: string
      description: ''
      default: ''
    user:
      type: string
      description: 'your user/group key (or that of your target user), viewable when
        logged into our dashboard; often referred to as USER_KEY in our
        documentation and code examples '
      default: ''
      nullable: false
  required:
    - token
    - user
