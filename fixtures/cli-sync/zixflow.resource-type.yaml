description: |-
  1. Click on "account settings" at the bottom-left corner of the screen.
  2. Inside settings, open "developer", and create a new API key.
  3. Copy the API key
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    apiKey:
      type: string
      description: API Key generated inside the settings dashboard.
  required:
    - apiKey
