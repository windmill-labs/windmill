description: >-
  1. On the dashboard, click the avatar/initials in the top-right corner to open
  up a dropdown and select `Platform API Tokens`.

  2. Create a new API token, copy it.
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    apiToken:
      type: string
      description: API token obtained from the Turso dashboard.
  required:
    - apiToken
