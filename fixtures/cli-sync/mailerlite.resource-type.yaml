description: |-
  1. Open up `Integrations` from the navigation bar and then select `API`
  2. Generate a new API token and copy it
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    apiToken:
      type: string
      description: API Token for authenticating the MailerLite API request
  required:
    - apiToken
